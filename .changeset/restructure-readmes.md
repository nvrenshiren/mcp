---
"@dawipong/mcp-pixabay": patch
---

Restructure READMEs: drop the quick-start section from the root READMEs (moved into each package) and expand the package quick start to cover npm/pnpm/npx install + configs for 6 mainstream AI tools (Claude Desktop, Cursor, Claude Code CLI, Windsurf, OpenCode, Codex CLI). Docs-only — no code or API changes.
